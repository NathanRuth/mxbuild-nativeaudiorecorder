PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_IDENTIFIER)$(BUNDLE_IDENTIFIER_APPEND)

BUNDLE_DISPLAY_NAME = NativeAudioRecorder
BUNDLE_IDENTIFIER = com.nativeaudiorecorder.mobile

RUNTIME_URL = http:\/\/localhost:8080
